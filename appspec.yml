version: 0.0
os: linux
files:
  - source: /artifacts
    destination: /var/opt/tapanda/bundles_tmp
hooks:
  ApplicationStart:
    # 'tapanda' user should be able to run code
    - location: scripts/cd_collectstatic.sh
      timeout: 600
      runas: tapanda
