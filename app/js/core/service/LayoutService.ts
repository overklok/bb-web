import LayoutMode from "../layout/LayoutMode";

export default class LayoutService {
    private modes: Map<string, LayoutMode>;

    public addMode(key: string, mode: LayoutMode) {

    }

    public compose() {
        // Inject Root View
        // Generate all Panes needed
    }
}