export default {
    modes: {
        default: {
            panes: [
                {
                    orientation: 'horizontal',
                    panes: [{orientation: 'vertical'}, {orientation: 'vertical'}]
                }
            ]
        }
    }
}